procedure main()

   // /* some comment*/
   // procedure main()
   //     ? "hello"
   // return

   // Single line comment
   LOCAL x := 5  // trailing comment
   ? "x=" + Str( x )
   * Star comment
   NOTE Old comment
   x := x /*x for sex is NB*/ + 1 && dBASE commentd
   ? "x=" + Str( x )
   /* Standalone
      multi-line comment */
   SET COLOR TO "W/B"

   ? "Amazing success!"

return
